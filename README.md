# planner-admin

Веб-клиент для тренера. Позволяет управлять расписанием, учениками и катками.

Клиент обращается к REST API бэкенда [planner](../planner).

---

## Запуск

Статических зависимостей нет — только HTML + Vanilla JS.

```bash
cd /Users/evgeny/Projects/telega/planner-admin
python3 -m http.server 3000
```

Открыть в браузере: `http://localhost:3000`

Бэкенд должен быть запущен на `http://localhost:8080`:

```bash
cd /Users/evgeny/Projects/telega/planner
./gradlew run
```

---

## Структура

```
planner-admin/
├── index.html          — единственная HTML-страница (SPA)
├── css/
│   └── style.css       — все стили, mobile-first
└── js/
    ├── api.js          — fetch-обёртка, хранение JWT в localStorage
    ├── auth.js         — экран входа / выхода
    ├── app.js          — инициализация, навигация по вкладкам, модальные окна
    ├── venues.js       — CRUD катков
    ├── students.js     — CRUD учеников, копирование и сброс токена
    └── schedules.js    — расписание: навигация по неделям, слоты, публикация
```

---

## Интерфейс

Приложение адаптировано под мобильный телефон (тренер работает с телефона). Оптимальная ширина — до 480px, на десктопе центрируется.

**Экран входа** — отображается при отсутствии JWT в `localStorage`. Поля email и пароль.

**Основное приложение** — три вкладки в нижней навигации:

| Вкладка | Содержимое |
|---|---|
| 📅 Расписание | Навигация по неделям. Создание расписания, добавление слотов, публикация |
| 👤 Ученики | Список учеников, копирование токена для бота, сброс токена |
| 🏟 Катки | Список площадок для тренировок |

### Расписание

Навигация стрелками `‹ 16 мар – 22 мар ›`. Для каждой недели возможны три состояния:

- **Нет расписания** — кнопка «Создать расписание»
- **DRAFT (черновик)** — список слотов, добавление новых слотов, кнопка «Опубликовать»
- **PUBLISHED** — только просмотр, счётчик `бронирований / вместимость`

### Ученики

Для каждого ученика отображается токен (первые 12 символов). Кнопка «Копировать» помещает полный токен в буфер обмена — токен передаётся ученику для входа через бот.

---

## Конфигурация

Адрес бэкенда задан в начале `js/api.js`:

```js
const API_BASE = 'http://localhost:8080';
```

При деплое передаётся через переменную окружения `PLANNER_API_URL` — см. раздел Docker ниже.

---

## Аутентификация

JWT сохраняется в `localStorage` под ключом `jwt`. При ответе `401` происходит автоматический выход. Токен действителен 7 дней.

---

## Docker

```bash
docker build -t planner-admin .
docker run -p 3000:80 -e PLANNER_API_URL=https://api.example.com planner-admin
```

Или через docker-compose (предварительно собрав образ):

```bash
docker compose up
```

При старте контейнера `docker/entrypoint.sh` подставляет `PLANNER_API_URL` в `js/api.js`. Один образ можно использовать на разных окружениях.

---

## CI/CD (GitHub Actions)

Workflow `.github/workflows/deploy.yml` запускается при пуше в `master`:

1. Собирает Docker-образ
2. Копирует его на сервер по SSH (`scp`)
3. Перезапускает контейнер с новым образом

Необходимые secrets в настройках репозитория (Settings → Secrets and variables → Actions):

| Secret | Описание |
|---|---|
| `DEPLOY_HOST` | IP или домен сервера |
| `DEPLOY_USER` | SSH-пользователь |
| `DEPLOY_SSH_KEY` | Приватный SSH-ключ |
| `PLANNER_API_URL` | URL бэкенда, например `https://api.example.com` |
